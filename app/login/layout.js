/**
 * Login Layout
 * 
 * Layout component for the login section that handles authentication and layout.
 * 
 * Location: /app/login/layout.js
 */

export default function LoginLayout({ children }) {
  return (
    <div className="min-h-screen">
      {children}
    </div>
  );
} 